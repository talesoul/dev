trigger chk on Contact (after update) 
{
   /* if(Trigger.isAfter)
    {
        for(Contact c : Trigger.New)
        {
            //Contact fg = new Contact();
            c.LastName = 'Smith';
           // update c; 
        }
         
    } */
      
}